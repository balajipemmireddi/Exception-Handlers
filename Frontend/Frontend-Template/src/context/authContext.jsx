// authContext.jsx — React Context definition for auth state.
// Consumed via useAuth hook (src/hooks/useAuth.js).

import { createContext } from "react";

export const AuthContext = createContext(null);
